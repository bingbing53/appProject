import Vue from "vue";
import Vuex from "vuex";
import axios from "axios";

Vue.use(Vuex);

const getters = {
 
};

export default new Vuex.Store({
  state: {
   
  },
  getters,
  mutations: {
   
  },
  actions: {
    //初始化
    init({ commit }) {
    
    }
  }
});
